Files Modified:

1. poker_types.py (renamed from types.py to avoid conflicts):
- Added BOMB_POT_ANTE = 1 and BET_CAP = 100
- Enhanced PlayerObservation with fields: hole_cards, my_stack, all_stacks, button_position, pot_total, hand_number
- Updated TableObservation to support None for empty seats
- Changed Rewards from int to float for negative bankroll tracking
2. gym_env.py:
- Added num_players parameter to __init__() (default 6)
- Completely rewrote reset() for 6-player bomb-pot rules
- Rewrote _get_single_player_obs() to return new PlayerObservation format
- Updated _get_valid_actions() for 6-player betting (streets 0, 1, 2+)
- Implemented button rotation across hands
- Added support for null observations for empty seats
3. engine_test_6player.py (new file):
- Created comprehensive test suite with 3 passing tests:
    - test_reset_6_players_full_table() - verifies 6-player initialization
    - test_reset_4_players_with_empty_seats() - verifies null seats
    - test_button_rotation() - verifies button advances correctly

Expected Behaviors Defined:

6-Player Reset:
- ✅ Deals 5 hole cards to each active player
- ✅ Deals 15 community cards (hidden during street 0)
- ✅ Forces $1 bomb-pot from all players
- ✅ Sets street to 0 (card selection phase)
- ✅ Returns 6-tuple of observations (None for empty seats)
- ✅ Tracks net gain/loss (stacks start at -$1 after bomb-pot)
- ✅ Button rotates each hand (hand 0 → button at 0, hand 1 → button at 1, etc.)

---
Next Steps: Card Selection & Betting

For test-driven development, here are the expected behaviors you should test next:

Phase 2: Card Selection (Street 0)

Expected Behavior:
- Players see all 15 community cards (3 boards × 5 cards each)
- Each player selects 2 cards from their 5 hole cards to keep
- Invalid selections (wrong count, invalid card indices) → player folds
- After all players select, advance to street 1 (betting)

Test Cases Needed:
1. Valid selection: Player selects 2 valid cards → cards stored in kept_cards
2. Invalid count: Player selects 1 or 3 cards → player marked as folded
3. Invalid card: Player selects card not in their hand → player marked as folded

Phase 3: Betting Round (Street 1)

Expected Behavior:
- Action order: left of button → clockwise
- Valid actions: CHECK, CALL, RAISE, FOLD
- Bet cap enforced (raises capped at BET_CAP)
- Round ends when all active players match highest bet
- Players can go negative (infinite bankroll)

Test Cases Needed:
1. All players check → proceed to showdown
2. Player bets, others call → proceed to showdown
3. All fold except one → that player wins without showdown
4. Bet cap enforcement → raises above cap rejected

Phase 4: Showdown (Street 2+)

Expected Behavior:
- Evaluate each of 3 boards independently
- Best 5-card hand from (2 kept cards + 5 board cards)
- Each board awards pot/3 to winner(s)
- Ties split that board's pot
- Rewards calculated as: (total winnings from 3 boards) - (total bet contributions)

Test Cases Needed:
1. Single winner on all 3 boards → wins full pot
2. Different winners per board → pot split 3 ways
3. Tie on one board → that board's pot split between tied players
4. All fold except one → winner gets full pot without showdown


Summary of Progress

  We've successfully implemented the card selection phase for the 6-player triple-board bomb-pot poker engine using test-driven development!

  ✅ Completed Implementation

  Phase 1: Types & Reset (Previously)
  - Defined PlayerObservation, TableObservation, and related types in poker_types.py
  - Implemented 6-player reset() with bomb-pot ante and button rotation
  - Community cards visible from the start (informed selection)

  Phase 2: Card Selection (Just Completed)

  Updated Files:
  1. gym_env.py - step() method:
    - Implemented Street 0 (card selection) logic
    - Action format: (card_idx_1, card_idx_2, -1) where indices are 0-4
    - Validates card selection (no duplicates, valid indices)
    - Invalid selections → player marked as folded
    - After all players select → advances to Street 1 (betting)
  2. engine_test_6player.py - Added 4 card selection tests:
    - test_community_cards_revealed_during_selection() ✓
    - test_card_selection_valid() ✓
    - test_card_selection_invalid_index() ✓
    - test_card_selection_duplicate_cards() ✓

  Expected Behaviors Verified

  Card Selection Phase:
  - ✅ Community cards are visible during card selection (informed selection)
  - ✅ Players take turns selecting 2 cards from their 5 hole cards
  - ✅ Action format: (card_index_1, card_index_2, -1) with indices 0-4
  - ✅ Invalid index (out of range) → player folded
  - ✅ Duplicate selection (same card twice) → player folded
  - ✅ After all active players select → game advances to street 1
  - ✅ kept_cards stored in observation for downstream use

  Test Results

  ✓ All reset() tests passed!
  ✓ All card selection tests passed!
  🎉 All tests passed!

  ---
  Next Steps

  Would you like me to continue with Phase 3: Betting Round implementation, or would you prefer to:
  1. Review the current implementation
  2. Add more edge case tests for card selection
  3. Work on a different part of the system (match.py, agents, etc.)

  The next phase would involve implementing the single post-flop betting round (street 1) with actions: FOLD, CHECK, CALL, RAISE.
